const crypto=require('crypto');const fs=require('fs');const path=require('path');
const USER='michi';const PASSWORD_SHA256='53fd9990be04790dd3289d0c35f8db544111fe79c7c9d90f11f51ff2ae8cf188';
function authorized(req){const auth=req.headers.authorization||'';if(!auth.startsWith('Basic '))return false;try{const decoded=Buffer.from(auth.slice(6),'base64').toString('utf8');const i=decoded.indexOf(':');if(i<0)return false;const user=decoded.slice(0,i),pass=decoded.slice(i+1),hash=crypto.createHash('sha256').update(pass).digest('hex');return user===USER&&crypto.timingSafeEqual(Buffer.from(hash),Buffer.from(PASSWORD_SHA256))}catch{return false}}
module.exports=function(req,res){res.setHeader('X-Robots-Tag','noindex, nofollow, noarchive');res.setHeader('Cache-Control','no-store, max-age=0');if(!authorized(req)){res.setHeader('WWW-Authenticate','Basic realm="FUORISEDE - Confronto Brescia 1", charset="UTF-8"');return res.status(401).send('Accesso riservato al confronto FUORISEDE.')}try{const html=fs.readFileSync(path.join(process.cwd(),'confronto-brescia-1.html'),'utf8');res.setHeader('Content-Type','text/html; charset=utf-8');return res.status(200).send(html)}catch(e){console.error(e);return res.status(500).send('Confronto temporaneamente non disponibile.')}};